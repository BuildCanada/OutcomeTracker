# Firebase MCP Server Environment Configuration
# Copy these variables to your system environment or load them before starting MCP clients

# Firebase Project Configuration
FIREBASE_PROJECT_ID=promisetrackerapp

# Optional: Explicit service account path (if not using default authentication)
# GOOGLE_APPLICATION_CREDENTIALS=./promisetrackerapp-firebase-adminsdk-fbsvc-6a44bafc23.json

# Optional: Enable Firebase emulator (uncomment for local development)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# Optional: Enable debug logging
# DEBUG=firebase:* 